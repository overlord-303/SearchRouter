{
  "name": "banger-searchrouter",
  "version": "1.1.1",
  "private": true,
  "scripts": {
    "dev":          "vite",
    "build":        "npm run build:main && npm run build:static",
    "build:main":   "tsc && vite build",
    "build:static": "tsc && vite build --config static/vite.config.ts"
  },
  "devDependencies": {
    "vite":            "^5.0.0",
    "typescript":      "^5.0.0",
    "vite-plugin-pwa": "^1.0.2"
  }
}
